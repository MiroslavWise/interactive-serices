import { TMyOffer } from "./types/types"

import { MotionLI } from "@/components/common/Motion"

import styles from "./styles/style.module.scss"

export const MyOffer: TMyOffer = ({ }) => {
  
  return (
    <MotionLI classNames={[styles.container]}>

    </MotionLI>
  )
}