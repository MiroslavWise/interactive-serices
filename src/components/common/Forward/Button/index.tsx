"use client"

import { forwardRef } from "react"

import type { TTypeButton } from "../types/types"

import { cx } from "@/lib/cx"

export const Button = forwardRef(function Button(props: TTypeButton) {
    
    return (
        <button
            
        >

        </button>
    )
})