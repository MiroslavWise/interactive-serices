import type { TMapCardNews } from "./types"

import { cx } from "@/lib/cx"

import styles from "./style.module.scss"

export const MapCardNews: TMapCardNews = () => {

  return (
    <div className={cx(styles.container)}>

    </div>
  )
}