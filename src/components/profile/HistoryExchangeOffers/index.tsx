import { THistoryExchangeOffers } from "./types/types"

import styles from "./styles/style.module.scss"

export const HistoryExchangeOffers: THistoryExchangeOffers = ({ }) => {
  
  return (
    <aside className={styles.container}>

    </aside>
  )
}