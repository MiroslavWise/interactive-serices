// "use context"

// import { useState, createContext, ReactNode, SetStateAction, Dispatch } from "react"

// export const MapContext = createContext<[any, Dispatch<SetStateAction<any>>]>([undefined, () => {}])

// function Context2gisProvider({ children }: { children: ReactNode }) {
//   const state = useState(undefined)

//   return <MapContext.Provider value={state}>{children}</MapContext.Provider>
// }

// Context2gisProvider.displayName = "Context2gisProvider"
// export default Context2gisProvider
